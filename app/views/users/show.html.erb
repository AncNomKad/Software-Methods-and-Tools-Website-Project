<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  <h2>
  <%= @user.email %>
  </h2>    
  </aside>
</div>
<%= link_to "Edit your profile", edit_user_path(@user) %> |
<%= link_to 'Back', root_path %>
